name: Minimal Test Workflow
on:
  workflow_dispatch: # Allows manual triggering
  push:
    branches:
      - master

jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a message
        run: echo "Hello from Minimal Test Workflow!"
