name: Debug Test Action

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  debug_job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a message
        run: echo "Debug action is running!"
